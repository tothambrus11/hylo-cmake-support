# Limitations
- Doesn't support multi-config generators (yet). Each separate configuration should be in a separate build directory.
- Only Ninja and make are supported as generators. VS and Xcode would require upstream work.